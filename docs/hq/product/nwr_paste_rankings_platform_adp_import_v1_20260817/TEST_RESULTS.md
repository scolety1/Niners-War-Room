# Test results

Focused owner-paste parser, snapshot, activation, provider-priority, original-text receipt, and CPU-label coverage passed in an isolated temporary directory. Python compilation and TypeScript typecheck passed. Full pytest was unavailable because the local offline environment did not contain pytest.
