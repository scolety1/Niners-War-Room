# Protected and frozen path proof

The complete candidate path inventory is limited to:

- `app/main.py`, `app/navigation.py`, and two Draft Cockpit root wrappers;
- `scripts/streamlit_runtime_cycle.py`;
- three direct navigation/runtime test files;
- this targeted packet.

No diff exists under `.github`, `.codex`, security automation/control files, source registries, identity authority, Data Health services, trust/refresh components, spreadsheet-safe CSV encoding, ranking/formula/scoring code, production data, LocalData, frozen comparator/research artifacts, or parked roster/plugin/rookie packets.

Whole-tree Ruff statistics are equivalent by rule/count between RC and candidate: 4,443 pre-existing findings and zero new finding. Changed-file Ruff is green. `git diff --check` and cached-diff check are required immediately before final commit/amend.
