# Shared contracts

All Phase 2+ contracts are immutable receipt-bearing read models: identity, scope, source/provider, terms receipt, fetch time, coverage, freshness, fallback, confidence, warnings and authority reference. Separate `EvidenceReceipt`, `FreshnessStatus`, `LeagueStateSnapshot`, roster/availability contracts, Redraft values and Dynasty signals. Recommendations cannot suppress missing/stale evidence.
