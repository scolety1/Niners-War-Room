# Opaque and Persistent State Preservation

Recommendation-adjacent and hidden-sort fields are rejected before lookup
construction. Render checks found no automatic winner, recommended side, or
accept-trade instruction. The behavior classification is
`MANUAL_DESCRIPTIVE_ONLY`.

The normal AppTest route snapshots the complete synthetic artifact tree before
and after page open and proves equality. A disposable source mutant that wrote a
marker into the authority root was killed by this assertion. No production
persistent user state, draft state, rankings, roster state, or generated artifact
is written by the repaired page-open path.
