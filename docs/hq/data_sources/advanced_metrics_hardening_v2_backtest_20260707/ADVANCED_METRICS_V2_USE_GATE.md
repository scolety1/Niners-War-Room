# Advanced Metrics V2 Use Gate

NGS passing/rushing/receiving are `GREEN_DISPLAY_ONLY_CANDIDATE` and `GREEN_SHADOW_BACKTEST_CANDIDATE` for review-only work, but not model-approved.

ffopportunity is `YELLOW_SOURCE_COVERAGE_PARTIAL`.
PFR advanced and ESPN QBR are `RED_IDENTITY_UNSAFE`.
FTN charting is `RED_SEMANTICS_UNSAFE`.
Routes, TPRR, YPRR, and `rz_att` remain blocked.
Exact PFF Elusive Rating is `RED_BLOCKED_PROPRIETARY`.
