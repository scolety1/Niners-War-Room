# HQ Advanced Metrics Elusive Proxy Note

Exact PFF Elusive Rating classification: `RED_BLOCKED_PROPRIETARY`.

No PFF data was scraped, copied, or used. Do not call any public proxy PFF Elusive Rating.

A future public proxy may be named only `nwr_elusive_proxy_review_only` and only after source inputs are admitted. Candidate formula, if legal inputs exist:

`((broken_tackles_or_missed_tackles_forced) / touches) * yards_after_contact_per_attempt * 100`

Required metadata before use: formula, input columns, source provenance, missingness rate, position scope, and warning that it is not PFF Elusive Rating. This lane did not approve the proxy for production rankings.
