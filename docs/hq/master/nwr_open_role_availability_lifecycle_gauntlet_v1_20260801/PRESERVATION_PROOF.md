# Preservation Proof

Phase 3 writes only this governed packet, its deterministic evaluator, tests, and Golden Lane closeout metadata. The evaluator reads tracked authorities and writes generated evidence only when `--write-generated` is explicitly supplied.

Finished V1, Outcome V3, Model V4 Rookie Review, the frozen comparator, active-pack pointers, formulas, rankings, application code, generated data packs, persistent state, recovery state, provider adapters, secrets, and scheduler state are outside the mutation allowlist and remain unchanged.

The operational checkout is not updated. The named refresh task remains disabled. Availability and lifecycle are guardrails; neither becomes a hidden score, penalty, or recommendation.

