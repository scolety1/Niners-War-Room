# Next Gate Recommendations

## Safe now

- Use this packet for review-only evidence that 2024 same-season Outcome labels have deterministic overlap with observed-row scoring components.

## Still blocked

- Global scoring parity until complete zero-row policy is implemented without converting missing player-week rows to zero.
- Return/special touchdown subtype parity until an unambiguous source mapping exists.
- Anchor-horizon parity using same-season scoring rows because the label window differs.
- Any model/training/source-truth use.

## Recommended next lane

Create a zero-row completion feasibility gate that proves a complete observed player-week denominator without treating missing rows as zero. Separately, investigate whether any approved source can disambiguate return touchdowns from other special-teams touchdowns.
