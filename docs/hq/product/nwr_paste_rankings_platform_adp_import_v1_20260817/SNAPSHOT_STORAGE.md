# Snapshot storage

Per-profile snapshots are local under `redraft/adp_provider_cache/owner_paste`. The JSON receipt contains parsed rows and activation state; the matching `.md` file retains the original owner text. Saving does not activate; clear deactivates without deleting the receipt.
