# Sunday Decision Sheet — 403 N 18th and friends

## Verdict: BLOCKED for lineup and pickup advice

Re-confirmed live this session (Worker 6 closure pass), against the
final-HEAD Redraft app (`1149b89d`): activating the real 403 N 18th
profile (`4b4a990faf124ce7a5d612537ba5943b`) and opening Start/Sit shows,
verbatim, in-app: **"Sleeper league required — Weekly in-season tools
(Start/Sit, Waivers, Trade, streamers) require an active Sleeper-imported
league. Choose or import one."** Same honest, existing, self-reported
refusal as KHA — not a silent failure, not stale draft data presented as
current.

## Exact reason

- **No ESPN Fantasy API integration exists anywhere in this codebase**
  (same finding as KHA, reconfirmed independently across this cycle).
- 403 N 18th is a real ESPN league (ESPN league id `1009373442`, 8 teams,
  PPR, 1QB). The only current data this app holds for it is a real,
  complete **118-pick historical draft board** — draft data, not a
  current roster. This league has more post-draft NWR-internal activity
  recorded than KHA (a `prospective_decision_log/`, ADP-refresh cache
  entries) but **still zero post-draft roster/transaction data from ESPN
  itself.**
- League status badge correctly shows **IN SEASON** (this cycle's own D2
  fix, live-reconfirmed this session).
- **Standing, separate gap (not fixed this cycle, unrelated to the ESPN
  block itself):** this worktree's own profile JSON has
  `provider_league_id: null` — the real ESPN league id `1009373442` is
  documented only in this project's memory/ledger and in a dated receipt
  file inside the real native AppData install, not in a machine-readable
  field in this worktree. If a future ESPN integration is built, that id
  needs to be supplied again from this record.

## Exact input checklist (documentation only — nothing built or attempted)

Identical checklist to KHA (see `03_kha_high_stakes.md` for full detail):

1. Real current roster read — manual local re-entry, or a genuine new
   ESPN API integration.
2. Real scoring config re-confirmation — `practical_mode: True` covers
   K/DST manual entry already; `draft.roster_limits` is empty (`{}`) in
   this worktree's copy, no per-position cap recorded, would need
   re-entry or a live ESPN settings read.
3. Real transaction/waiver state — none exists from ESPN anywhere in this
   codebase.
4. If pursued: a genuine, new, read-only ESPN Fantasy API client
   requiring the owner's own `SWID`/`espn_s2` session cookies, supplied
   explicitly by the owner — never scraped, requested, or extracted by
   this project (hard boundary).

## What this sheet cannot do

No lineup, pickup, K/DST, or FAAB advice is provided for 403 N 18th, for
the same reason as KHA.
