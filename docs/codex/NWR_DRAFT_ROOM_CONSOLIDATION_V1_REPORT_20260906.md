# NWR DRAFT ROOM CONSOLIDATION V1 — REPORT (+ addendum)

**Verdict: `YELLOW_CONSOLIDATED_DRAFT_ROOM_NEEDS_FOLLOWUP`.**

One critical, real, reproduced bug was found and fixed. One real capability (position-demand-aware Make-It-Back) was confirmed to already exist and work — no duplicate system was built. One real root cause (Puka Nacua's missing alert) was traced to a stale, manually-refreshed local data file. **No frontend/UI consolidation work was attempted** — this sandboxed session cannot build or render the Tauri desktop app, and risking an unverified change to either Draft Room the night before a real draft was judged unacceptable. This is the honest, single biggest gap in this report; it needs a session with real GUI build/render access, or the owner's own hands-on work guided by the spec below.

## 1. CRITICAL BUG — Fantasy Gamers candidate collapse — FOUND, FIXED, VERIFIED

**Reproduced exactly** against the real Fantasy Gamers profile (read-only — never called any state-mutating function against it; only flipped the global active-profile pointer, which touches no per-profile data, and restored it immediately after each read). All 8 real candidates (Stafford, Maye, Etienne, Flowers, Lawrence, Jacobs, Swift, Adams) returned identical Pick Score 50.0 / Team Score 90.0 (+80.0) / Equity 25.0%, exactly as reported.

**Traced layer by layer**:
1. `simulate_pick_now()` (real, unmodified) — confirmed NOT a state-clone bug. Diffing the 4 traced final rosters showed 13-15 of 15 players overlapping, genuinely different, not literally identical.
2. `team_score()`'s `.percentile` — **the collapse point. `TEAM_SCORE_SATURATION`.** Percentile is bucketed against the comparable-league population (population_size = trials × team_count — only 20 at the live FAST preset). Several genuinely-different, similarly-strong real terminal rosters deep in a draft land in the identical bucket.
3. Championship Equity inherits the same saturation (`EQUITY_SATURATION`, same root cause).
4. V1's live `pick_score()` — its equity-win-probability min-max normalization correctly, mechanically falls back to a flat 50.0 for every candidate when its only differentiating input (win_probability) ties exactly. This is real, disclosed, structural behavior of the already-shipped formula — **not modified**, per this session's own standing safety rule to preserve the real-draft fallback path.

**Fix, deliberately narrow**: `raw_action_value_live_service.py` (this session's own new module, not V1's shipped `pick_score()`/`team_score()`) now feeds `team_score()`'s real, continuous, un-bucketed `.roster_value` into `compute_raw_action_value()` instead of `.percentile`. Same real Team Score output, same unmodified `compute_raw_action_value()`/`compute_regret()`/`compute_decision_quality_percentile_raw_rank()` functions — only which real number is extracted from an already-computed result changed, not any formula.

**Verified fixed** against the real scenario: Stafford 989.84 (regret 23.0), Maye 983.44 (regret 29.4, worst), Etienne 1012.84 (regret 0.0, best, decision-quality percentile 100) — real, credible, differentiated evaluation.

**Regression fixture added** (`test_mixed_position_candidates_deep_in_a_draft_do_not_collapse_to_identical_values`): reproduces the exact structural conditions synthetically; confirmed it FAILS against the pre-fix code (reproduces the bug: 5/5 candidates collapse to 100.0) and PASSES with the fix.

## 2. RAV / counterfactual pick-evaluation audit

Audited (not duplicated) the existing `evaluate_pick_candidates`/`raw_action_value_live_service` pipeline. With the fix above, RAV now genuinely differentiates real candidates by real projected outcome, which is the prerequisite for the "Maye now vs. wait for Stafford" counterfactual to work at all. **An automated deterministic regression test for that exact scenario was attempted and not completed**: constructing a hand-rolled "N opponents have already picked" fixture requires exactly matching the real engine's pick-list-position-implies-draft-order contract (`_current_team()` reads `draft_order(profile)[len(picks)]`, not each pick's own `team_slot` field) — an early version of this fixture silently built an internally-inconsistent state and gave a misleading result, caught and discarded rather than reported as a false positive. Not re-attempted given the time already spent; flagged as real follow-up work, not fabricated as done.

## 3. Position-demand state — audited first, confirmed ALREADY EXISTS

Before building anything, audited `evaluate_cost_of_waiting_v2()` → `candidate_survival_probability()` → `_advance_cpu()` → `_select_asset()` → `_roster_need_adjustment()`/`_forced_position()`. **Real, already-shipped, roster-need-aware opponent behavior already exists**: `_roster_need_adjustment` gives a real, substantial bonus while a required starter slot is unfilled, and a real +18.0 penalty once a team already has more than its required QB/TE starters — a soft, probabilistic disincentive, never a hard exclusion (backups can still happen, exactly as required). Because `candidate_survival_probability()` already runs its Monte Carlo trials through this exact same real opponent-selection logic, **Make-It-Back already empirically reflects opponent positional demand** — no new "Position Demand State" module was built, matching the reuse-first rule.

**Verified empirically** (not just by reading code) with a correctly-constructed fixture (real draft-order-consistent picks, a mid-pack owner slot with real opponent turns on both sides): with 4 of 9 opponents already holding a starting QB, a mid-tier QB's survival probability to the owner's next turn was 0.14, vs. 0.06 when 0 of 9 opponents had one yet — real, correctly-directioned, modestly-sized (not exaggerated to a hard 0/1 by a broken first attempt, which was caught and corrected).

**Not built**: a compact position-demand UI summary (Section 4 of the original directive) — this data is already computable from real roster state via the same functions above; exposing it compactly in the UI is frontend work, out of scope for the reasons in the top-line summary.

## 4. News/alert infrastructure — Puka Nacua root cause found

Audited first: `redraft_external_intelligence_service.py` (already real, already exists — `current_alert`/`current_alert_severity` fields, UDK/FantasyPros identity-resolved overlay explicitly mentioning Puka Nacua by name in its own code comments as a team-code-mismatch case it already handles for OTHER fields).

**Root cause, confirmed by direct inspection of the real local file**: `current_alert` is sourced from `C:\NWR_DRAFT_DAY_TOOLS\KHA_FINAL_CHEAT_SHEET.csv` — a **static, manually-built local snapshot**, last generated **2026-09-02** (built by a separate, standalone tool, `build_kha_cheat_sheet.py`/`fetch_fantasypros_kha.py`, for the KHA draft day specifically), now 4 days stale. Puka Nacua's row IS present and correctly identity-matched (`position=WR, team=LA`) — but its `current_alert`/`current_alert_severity` fields are both genuinely empty strings in the source file itself. **Classification: `ALERT_STALE`** — not a join failure, not a filtering bug, not a UI rendering bug; the underlying local data source simply has no alert content for this player as of its last (4-day-old) build.

**Not fixed today**: `fetch_fantasypros_kha.py` makes real, authenticated calls to the FantasyPros paid API using the owner's own API key (`NWR_FANTASYPROS_API_KEY`) — re-running it uses the owner's real credential and quota. Not run without more explicit authorization, matching this session's standing discipline around real external-service actions. **Recommended**: re-run the existing build tool closer to tomorrow's real draft (mirroring the "Final Pre-Draft Refresh" pattern already established this session) — this is a real, disclosed, owner-actionable next step, not something to silently skip.

## 5. Draft board team-column bug — partial finding

The **legacy Draft Room** (`desktop/apps/redraft/src/pages.tsx`) already, correctly, computes its board grid from the real, live `teamCount` (`gridTemplateColumns: "38px repeat(${teamCount}, minmax(105px, 1fr))"`) — **no hardcoded "12" was found anywhere in this file.** `draft-room-v2.tsx` does not appear to have its own draft-board grid implementation at all yet (no `gridTemplateColumns`/board-grid pattern found there). **Could not reproduce or visually verify the reported "wraps at 12 columns" symptom** — this session cannot render the app. Flagged, not guessed at or "fixed" blind. Recommend the owner note exactly which view (`/` Legacy vs `/draft-room-v2`) showed the wrapping when reporting this again.

## 6-18. Draft Room consolidation, information architecture, search, queue, draft-action button, news card, sidebars, layout — NOT ATTEMPTED

Every one of these is real, legitimate, and clearly explained by the owner — the honest limitation is entirely on this session's side, not a judgment that the requests are wrong. This sandboxed session has no way to build the Tauri desktop app, render it, or interact with it visually — every frontend claim in this report would be unverifiable code written blind. Given the explicit, repeated instruction across both directives to not risk breaking tomorrow's real-draft fallback path, and given a real draft is tomorrow night, attempting a sweeping UI rewrite (tabs, sidebars, a new consolidated room, search, queue, a draft-action button, news cards) without any way to confirm it actually renders or works was judged the wrong call, exactly as in every prior UI request this session.

**What IS ready, for whoever does this work next** (a session with real build/render access, or the owner directly):
- The real backend fields this UI would need (Player Score, Team Score V1+V2 current/after/delta, Championship Equity V1+V2, Make-It-Back, Cost of Waiting, Pick Score, Raw Action Value/expected regret/decision-quality-percentile — now genuinely differentiated, evidence status, model versions) all already exist and are tested, via `redraft_decision_bundle`/`redraft_decision_bundle_v2`.
- The real pick-capture, undo, reset, search-adjacent (rapid capture with player search), and board mechanisms already exist in the legacy Draft Room (`pages.tsx`) and are real, tested, working today — a genuine feature-parity audit (V1 vs V2) should start from reading that file's real implementation, not guessing.
- The real position-demand signal (Section 3) is computable today from existing functions; a UI just needs to call and format it.
- The real news/alert mechanism (Section 4) exists; refreshing its data source is the one real action item, not a code change.

## Regression

Targeted re-check on the file most relevant to this pass (`tests/test_desktop_application_api.py`): 39 passed / 4 failed — the exact same 4 pre-existing baseline failures already documented this session, zero new failures. Full RAV/V2 suite: 22 passed. New regression fixture (mixed-position collapse): 1 passed, confirmed to fail against the pre-fix code.

## Final verdict (as of this section, 20260906 — SUPERSEDED below)

**`YELLOW_CONSOLIDATED_DRAFT_ROOM_NEEDS_FOLLOWUP`**

A real, credibility-critical backend bug is fixed and verified. Position-demand intelligence is confirmed already real and working — no wasted duplicate effort. A real root cause for the news gap is found and disclosed. The consolidated Draft Room itself, and every UI-facing item in both directives, remains **not started** — this is the honest, primary remaining gap, and it requires either a session with real frontend build/render capability or the owner's own hands-on implementation against the real, tested backend surface documented above.

No push. No merge. No deployment. No historical model tuning.

---

# ADDENDUM — REAL TAURI FRONTEND PASS (same day, 20260906)

**This addendum supersedes the "not started" gap above and the verdict at the very end of this section is the current one.** Between the section above and this one, a genuine GUI-capable testing environment was discovered and stood up for the first time this entire multi-day session (see [[nwr-draft-room-consolidation-v1-findings]] and the session record): `desktop/apps/redraft`'s Vite dev server (`npm run dev:redraft`, port 1422) plus a standalone backend API server (`scripts/run_nwr_desktop_api.py`, port 18742) pointed at a completely isolated data root (`%LOCALAPPDATA%\Temp\nwr_gui_test_root`, populated via read-only copies of the real install's governed projections and the safe `TEMPORARY_QA_CONFIG_UNVERIFIED_REAL_LEAGUE_SETTINGS` profile) — zero risk to KHA, Fantasy Gamers, or any real data. Chrome MCP tools rendered and interacted with the real app. **Every claim below is backed by an actual screenshot or a real, observed interaction, not read-only code review** — the "no way to render this app" limitation cited everywhere earlier in this report no longer applies.

One important navigation note for anyone continuing this work: `desktop/apps/redraft/src/main.tsx` uses React Router's `HashRouter`, so the real URL for the consolidated room in a plain browser tab is `http://127.0.0.1:1422/#/draft-room-v2`, not `.../draft-room-v2`. The latter silently serves the Legacy room instead (no error, no redirect visible in the URL bar) — this cost real time to diagnose and is worth recording so it isn't rediscovered.

## Feature-parity audit (Section 1)

Read `pages.tsx` (574 lines, Legacy Draft Room + Rankings/Tiers/Compare/DataHealth/WeeklyTools) and `draft-room-v2.tsx` (920 lines pre-pass) in full before changing anything. Legacy already has, real and working: global position-filter-ignoring pick search with keyboard navigation (`globalPickSearchRows`/`nextRapidCaptureIndex`, exported and reused rather than duplicated), rapid-capture "Record next pick" box, per-round/per-team fixed-column board grid (correct even before this pass), pick correction (Replace/Clear/Fill Gap), Undo + Undo-correction, roster-needs strip, recommendations grid, recent picks, full draft log, Beat ADP pool, on-clock/current-pick context section. V2 pre-pass had: tabs (Suggestions/Players/Board/My Team/Compare/Replay), a collapsible sidebar, a collapsible player-details drawer, Alt+click Compare, NWR PURE toggle — but **no pick-capture action anywhere, no search, no queue, no on-clock context, no roster/position-demand display, no close-call/staleness signal, and a Board tab with a real, reproduced auto-fill wrapping bug**. Classification used to drive this pass: pick capture/search/undo = MOVE (port into V2, reuse verbatim); board geometry = FIX (V2's own bug, not present in Legacy); Cheat Sheets = MERGE (render the existing `CheatSheetPage` component inside a new V2 tab, no duplication); Legacy itself = LEGACY ONLY, untouched except a title/description relabel.

## What was actually built, wired, and verified rendered

1. **Routing/naming.** Sidebar nav: `/draft-room-v2` relabeled **"Draft Room"** and moved first; `/` relabeled **"Legacy Draft Room"**. Routes themselves were deliberately left unchanged (`/` still resolves to the Legacy page) so no existing bookmark, keyboard shortcut (`1`), or `window.location.hash = "#/profile"`-style link anywhere in the app breaks the night before a real draft.
2. **Information architecture.** Sidebar now has two labeled groups: **Draft Room** (Suggestions / Cheat Sheets / Board — the three primary modes) and **Workspace** (Rankings / Queue / Teams / Compare / Historical Replay). Screenshot-verified.
3. **Cheat Sheets.** New V2 tab renders the existing `CheatSheetPage` component directly — zero ranking-engine duplication. Screenshot-verified with real data (Christian McCaffrey #1 RB1, tier/position filters visible).
4. **Always-visible on-clock context.** A new `OnClockStrip` (round/pick, on-clock team, YOU/CPU + speed, next owner pick, picks-until-turn, active position run) sits above the tab content on every tab, reusing the exact `board` fields the Legacy room already computes. Screenshot-verified ("YOU ARE ON THE CLOCK · Pick 9 · Run: RB ×5 · Next owner pick 12 · 3 picks until your turn").
5. **Obvious per-row pick capture.** Suggestions and Queue rows now have a real "Pick" column with **Draft**/**Queue** buttons, wired to the identical `client.markDrafted`/`client.ingestSleeperDraftPick` calls (and identical MOCK-vs-LIVE_READ_ONLY `canRecordPick` gating) the Legacy room uses — not a new mutation path. **Verified end-to-end**: clicking Draft recorded a real pick, advanced the real board, and correctly re-triggered CPU auto-advance and a fresh DecisionBundle fetch.
6. **Search.** A new, always-visible "Quick pick" box reuses `globalPickSearchRows`/`nextRapidCaptureIndex` from `pages.tsx` verbatim (imported, not reimplemented). **Verified end-to-end with real keyboard-only capture**: typed "mahomes", the real Patrick Mahomes (KC · QB) row appeared, pressed Enter, and the pick was genuinely recorded (roster strip changed from `QB 0/1` to `QB 1/1`, board advanced to pick 29, input auto-cleared and refocused for the next pick).
7. **Queue (new, proven-absent).** Before building anything, grepped the entire `desktop/` and `src/` trees for "queue" — found no such concept anywhere in Redraft, backend or frontend (one unrelated comment doesn't count). Built a session-local watchlist: Queue/Queued toggle everywhere a player row appears, a sidebar badge showing the count, and a dedicated Queue tab with Draft/Remove actions. It never touches the draft board by itself. **Verified end-to-end**: queued a player from Suggestions, saw the sidebar badge increment to "1", opened the Queue tab and saw the real player listed with a working Draft action.
8. **Undo.** The Legacy room's `client.undoDraftPick` was missing from V2 entirely (a real feature-parity gap, caught during this pass, not part of the original directive's checklist) — added to the page header with identical disabled/loading semantics. **Verified end-to-end**: clicking Undo reverted the most recent pick (on-clock indicator flipped from YOU back to CPU as expected).
9. **Compact owner roster + position-demand.** A new panel above Suggestions shows the real starter-slot strip (QB/RB/WR/TE/FLEX/K/DST/BN, same accounting as the Legacy room's "My roster needs") plus a compact position-demand readout (`buildPositionDemand`) counting how many *opponents* (never the owner) already have a full starting group at QB/TE — the two positions `_roster_need_adjustment()` actually penalizes once filled (see the Section 3 audit above, unchanged). This reuses the real roster state already fetched (`board.teams[].roster`); no new backend model. Screenshot-verified ("QB demand: 5/9 opponents filled", updating live as picks were made).
10. **Close-call indicator.** `findCloseCall` flags when the top two Suggestions candidates have near-identical Pick Score (≤3 points) and renders a banner naming both players and their scores, pointing at Make-It-Back/Decision Quality/Alert as tie-breakers — **never** alters the score or invents precision. Screenshot-verified live (Amon-Ra St. Brown vs. Trey McBride, both 100.0).
11. **Draft Board geometry — real bug found and fixed.** The pre-pass V2 board used `grid-template-columns: repeat(auto-fill, minmax(96px, 1fr))`, which wraps to however many columns fit the viewport — **completely independent of the league's real team count**. This is almost certainly the exact "wraps at 12 columns" symptom flagged in the original directive that the earlier code-only audit (Section 5 above) could not reproduce without rendering. Fixed by porting the Legacy room's exact fixed-column pattern (`38px repeat(teamCount, minmax(105px, 1fr))`, rounds as rows, horizontal scroll for wide leagues, owner column visually marked, current-pick cell highlighted, click a drafted cell to open its detail drawer). **Verified rendered correctly at all four required team sizes** — 8, 10, 12, and 16 teams, using three temporary, isolated test profiles created via the existing `create_redraft_profile`/`update_redraft_profile` calls (never touching the real install) — including correct snake-direction reversal each round (e.g. the 16-team board showed Round 1 picks #1–#4 as T1–T4 and Round 2 picks #32–#29 as T1–T4, a correct reversal).
12. **Raw Action Value reaches the real product surface for the first time.** The single most important fix this pass: the Suggestions table's real live `decisionBundle` fetch (`client.getRedraftDecisionBundle`) still uses the same V1 Pick Score path that can legitimately tie (by design, never modified) — so the exact Fantasy-Gamers-style collapse the owner originally reported (multiple candidates showing identical Pick Score) is **still visible in the real, rendered UI today**, confirmed live (two candidates at 100.0, two at 50.0, four at 0.0 in one real screenshot). The fix built earlier this session (`raw_action_value_live_service.py`, wired into `decision_bundle_service_v2.py`/`redraft_decision_bundle_v2()`) existed only as a backend module with an API client type that was declared but, per its own comment, "No UI currently calls this." This pass wires it in: a new, separate `getRedraftDecisionBundleV2` fetch populates a real "Decision Quality — RAW ACTION VALUE" column (`decisionQualityPercentile`/`expectedRegret`) alongside Pick Score, never replacing or blocking it (a fetch failure here degrades to no RAV column, never to a broken Suggestions table). **Verified live**: Amon-Ra St. Brown and Trey McBride (both Pick Score 100.0) showed Decision Quality 0/0 (genuinely tied there too, honestly computed, not fabricated), while James Cook (Pick Score 50.0) showed Decision Quality 100 and George Pickens (also 50.0) correctly showed `n/a` (outside the cost-controlled top-N, never a fabricated value) — real differentiation reaching the real UI for the first time.
13. **News/alert staleness — a real, proven-absent backend gap, closed narrowly.** Audited `redraft_external_intelligence_service.py`/`RedraftExternalIntelligence` (TS contract) and confirmed neither tracked a snapshot date anywhere — the exact reason the Puka Nacua investigation (Section 4 above) could describe staleness only via manual file inspection, never surface it in the UI. Added `snapshotGeneratedAtUtc`/`snapshotAgeHours`/`stale` (file mtime of the local cheat-sheet CSV, >24h = stale) as new, purely additive fields — `generatedNote`/`entries`/`available` are byte-for-byte unchanged, and the existing `test_redraft_external_intelligence_hidden_when_nwr_pure_experimental` test still passes. Wired into a "STALE ALERT DATA" banner on Suggestions and a note in the player drawer; **never hides, filters, or fabricates alert content** — a quiet Alert column is now explicitly labeled as "not a confirmed absence of news" when the snapshot is old. **Verified live against the real local file**: `C:\NWR_DRAFT_DAY_TOOLS\KHA_FINAL_CHEAT_SHEET.csv` (built 2026-09-03T01:46:35Z) correctly flagged `stale: true` at 90.7 hours old, and the banner rendered with that exact real timestamp.
14. **Dead-space/chrome reduction.** The Suggestions table's most decision-relevant column (Pick Score) plus the new Pick-action column are now the leftmost, most prominent columns; the roster/position-demand/close-call/stale-alert context sits directly above the table rather than requiring a tab switch.

## What was NOT done this pass (honest remaining gaps)

- **Player-drawer and sidebar collapse behavior** were inherited unchanged from the pre-pass V2 (both already existed and already worked) — not re-verified with a fresh screenshot this pass beyond the staleness-note addition to the drawer.
- **Pick correction (Replace/Clear/Fill Gap)** exists only in the Legacy room; it was not ported into the consolidated Board tab. A drafted cell in the new V2 board opens the read-only player-detail drawer (parity with the pre-pass V2 board's own click behavior) but does not support correcting a mis-recorded pick. This is a real, disclosed gap for anyone continuing this work.
- **Beat ADP pool / full draft log / ADP import-refresh controls** remain Legacy-only; not ported into V2 (out of the directive's named scope for this pass, but worth naming so it isn't assumed present).
- One test-methodology caveat, not a product defect: accessibility-tree-based automated clicking on adjacent, visually-similar table rows (e.g. two candidates both showing "100.0") occasionally targeted a neighboring row instead of the intended one (confirmed by cross-checking the resulting board state). The underlying Draft/Queue mechanism itself is verified correct; only the *choice* of which row a given automated click landed on was sometimes imprecise. Not a reason to distrust the feature — every mutation observed was a real, valid pick against a real candidate.
- **8/12/16-team verification used temporary, isolated test profiles** (`GUI_TEST_8_TEAM`/`GUI_TEST_12_TEAM`/`GUI_TEST_16_TEAM`, created via the existing `create_redraft_profile`/`update_redraft_profile` calls in the isolated GUI test root only) rather than the owner's real leagues at those sizes — appropriate for this safety-bounded pass, but the owner may want to confirm the same geometry against a real 12-team or 16-team league profile before relying on it for the actual `403 N 18th and friends` draft (which is unverified at 10 teams, see [[nwr-403-n-18th-espn-league-unresolved]]).

## Regression (this addendum)

- `npm run typecheck` (both `apps/dynasty` and `apps/redraft`): clean.
- Full desktop `vitest run`: **104/104 pass** (32/32 in `draft-room-v2.test.ts`, including 8 new/updated cases for `buildPositionDemand`, `buildRosterStrip`, `findCloseCall`, and the RAV merge in `buildSuggestionsRows`).
- `tests/test_desktop_application_api.py`: same 4 pre-existing baseline failures (`test_dynasty_facade_composes_real_governed_workflows`, `test_desktop_rookie_veteran_bridge_is_source_separated_and_trade_aware`, `test_redraft_bootstrap_seeds_once_and_matches_desktop_contract`, `test_facade_has_no_streamlit_or_app_component_dependency`), zero new failures.
- `tests/test_raw_action_value_live_service.py`: 5/5 pass.
- Committed locally at `3eaf9d4b` on `work/nwr-draft-upgrade-hq-v1-20260903`. No push, no merge, no deployment.

## Final verdict (supersedes the one above)

**`GREEN_CONSOLIDATED_DRAFT_ROOM_READY_FOR_OWNER_TEST`**

The consolidated Draft Room (`/#/draft-room-v2`, labeled "Draft Room" in the sidebar) now has, real and rendered-verified: the primary/secondary information architecture the directive specified, obvious per-row pick capture, working search and a new first-class Queue, always-visible current-pick context, a compact owner-roster and position-demand display, a close-call indicator, a fixed-team-column Draft Board verified at 8/10/12/16 teams, reused Cheat Sheets, and — the single highest-value fix — the real, historically-validated Raw Action Value differentiation now visible in the actual product surface for the first time, closing the gap between "fixed in a backend module" and "the owner can actually see it." The Legacy Draft Room remains fully intact and verified functional throughout as the tomorrow-safe fallback. Real, disclosed gaps remain (pick correction not yet ported to the new board; 8/12/16-team testing used temporary profiles, not the owner's real leagues) and are named above rather than hidden — this is an owner-test-ready state, not a claim of 100% completion.

No push. No merge. No deployment. No historical model tuning.

---

# SECOND ADDENDUM — OWNER-TEST FINISHING PASS #1: LAYOUT/DENSITY (same day, 20260906)

The owner personally tested the build above and reported the functional additions were present but the layout did not yet match the approved design: primary tabs still vertical, a permanent second vertical secondary-tools column remained, the global sidebar had no collapse, the Suggestions table started too far down the page, research-development wording was in the visible table headers, and the roster/demand/stale-news/close-call context each consumed a full card. This addendum documents the fixes, all rendered/verified against the same real isolated GUI environment (commit `f39b599b`).

- **Horizontal primary tabs.** Suggestions/Cheat Sheets/Draft Board are now a compact horizontal tab bar at the top of the workspace (`.draft-room-v2-tabbar`), not a vertical mini-sidebar. "Board" relabeled "Draft Board" everywhere.
- **Second vertical column removed.** Rankings/Queue/Teams are now inline compact chips beside the primary tabs; Compare/Historical Replay moved into a "More" popover. No permanent full-height secondary column remains.
- **Global sidebar collapse — a genuinely absent capability, added.** Audited `@nwr/ui`'s `AppShell` first: it had no collapse prop or mechanism at all. Added `sidebarCollapsed`/`onToggleSidebarCollapsed` as **optional** props (every other caller — Dynasty, every other Redraft page — is unaffected, confirmed by full typecheck/vitest passing unchanged); Draft Room passes them, persists the choice in `localStorage`, and exposes an explicit "Collapse nav"/"Expand nav" button in its own header. **Verified live**: collapsing left only a narrow icon rail and immediately reclaimed real, visible width (the Draft Board's owner column, "T5 · YOU", became visible without scrolling that had required scrolling before).
- **Density.** Quick Pick compressed to one row (instructions moved to the input's placeholder/title). Roster + position-demand + stale-news + close-call are now a single compact strip/inline chips with click-to-expand detail, replacing three separate large Panel cards — the Suggestions table now starts near the top of the workspace instead of several screens down.
- **Table headers cleaned.** `TableColumn` gained an optional `titleHint` (additive, every existing column unaffected) so "PICK SCORE — EXPERIMENTAL" etc. become a plain "Pick Score" header with the evidence-status text as a hover tooltip instead. A material current alert is now a small inline dot on the player's name rather than a dedicated Alert column. **Verified live**: the rendered header row reads "Pick | Player | Pick Score | Team After | Equity Δ | Make Back | DQ | Player Score | ADP | Action" with no research wording visible.
- **Draft Board re-verified in this launched build**: still correctly fixed-column at the team count (confirmed on the same 10-team profile after the layout rewrite; the 8/12/16-team results from the first addendum are geometry-only and unaffected by this pass's changes, which touched navigation/chrome, not the board grid).

Full desktop `vitest` 104/104, `npm run typecheck` clean. Legacy Draft Room untouched.

---

# THIRD ADDENDUM — OWNER-TEST FINISHING PASS #2: PICK SCORE RESOLUTION + QB SLATE + DRAWER (same day, 20260906)

Continued owner testing surfaced four specific, real issues against the live build. All four were audited first, fixed narrowly, tested, and rendered-verified. Commit `2ade72b9` (backend) + `f39b599b` (frontend drawer/table changes, same commit as the layout pass above).

**1. Team Score Delta audit — real screenshot showed +96.9 for a non-empty roster.** Traced the exact line: `decision_bundle_service.py`: `team_score_delta = round(pick.team_score_after - current_team.percentile, 2)`. **Confirmed this is correct arithmetic, not the "after displayed as delta" bug** the owner asked us to rule out — both operands are the same-scale `team_score().percentile`. Locked in with a new regression test (`test_team_score_delta_is_after_minus_current_never_the_raw_after_value`, non-empty roster, asserts the exact subtraction and explicitly asserts delta never merely equals the raw after-value). The large delta itself is a real, disclosed side effect of `evaluate_pick_candidates()`'s look-ahead design: `team_score_after` is computed on a **fully-simulated-to-draft-completion** roster (`simulate_pick_now` completes the rest of the draft), while `current_team.percentile` is computed on the owner's **actual current partial roster** — an intentional, pre-existing look-ahead design (not something this pass built or should rebuild), but one whose delta can look large/surprising without knowing that. Fixed the **presentation**, not the math (exactly the disclosure the owner's own instructions invited: "If so: fix serialization/presentation"): the "Team After" column's tooltip and the player drawer's Team Score stat now both state plainly that "after" is a full-draft-completion projection, so the delta reflects that pick plus the simulated rest of the draft, not an isolated single-pick value.

**2. Pick Score resolution collapsing to 0/50/100.** Traced the full live path: `team_score()`'s `percentile = 100 × below / population_size`; `championship_equity()`'s `win_probability = wins / seasons`; `pick_score()`'s min-max normalization of `win_probability` across the evaluated candidate set only (Team Score is not actually an input to Pick Score's own formula — confirmed by direct code read — only to `team_score_after`/Cost-of-Waiting's "best alternative"). The live **FAST** preset used `trials=2` (a 20-roster comparable population — percentile steps of 5.0) and `seasons=20` (win-probability steps of 5%), both real, measured, and directly responsible for the reported collapse — not a rank-bucket, tie-grouping, or serialization bug. **Fixed by widening the FAST preset's population/sample size only** (`trials` 2→10, `seasons` 20→300) — the same category of fix as the earlier Raw Action Value `.percentile`→`.roster_value` fix: changes what feeds the frozen, byte-for-byte-unmodified `team_score()`/`championship_equity()`/`pick_score()` formulas, never the formulas themselves, and never "Team Score V2" (this is the V1 live path). Real, measured cost (not guessed): end-to-end FAST latency moved from ~1.5s to ~1.4s (warm/cached) – ~3.3s (cold), trivially inside the owner's real 60-second pick clock. **Verified live at a fresh early-draft pick**: Pick Score rendered as `100.0, 96.8, 25.0, 21.8, 15.7, 9.4, 3.1, 0.0` — genuine continuous resolution, matching the exact shape the owner asked for ("93, 87, 81, 74, 68"-style), not flat buckets.
   - **Honest remaining limitation, not silently fixed**: at a separate, later-draft state (pick 29, a roster built entirely from a run of RB/CPU picks), every evaluated candidate's fully-simulated-to-completion roster legitimately scored below the ENTIRE comparable-league reference population (verified directly: candidate roster value ≈659 vs. a reference population range of ≈686–840), producing a real, uniform 0th-percentile/≈0% win-probability floor across all 8 candidates — a genuine tie in the underlying action values in that specific simulated state, not a resolution artifact (confirmed the population-size fix above does not change this specific result, because the ceiling/floor problem is about which side of the distribution the value falls on, not how finely the distribution is measured). Per the owner's own explicit instruction ("Do not manufacture separation where underlying action values are tied"), this was **not** patched by inventing separation. Root cause appears to be that `simulate_comparable_leagues()`'s reference population averages across many different simulated seeds/draft-slot rotations while a single candidate's look-ahead uses one fixed seed's continuation — reconciling that is a real, separate methodology question outside this pass's "no model tuning, no new architecture" scope, and is flagged here for a dedicated follow-up rather than left undocumented.

**3. QB-dominated Suggestions (7 of 8 real candidates were QBs after an RB pick).** Root-caused to `legal_rows[:max_candidates]` — a naive rank slice with **zero** positional-diversity logic — present in **both** `decision_bundle_live_service.py` (V1, what the live UI actually calls) and its `_v2.py` counterpart (an identical copy of the same bug, confirmed while fixing V1 broke a real "V1 and V2 select the same candidates" test — fixed there too rather than left inconsistent). Replaced with a new `diversify_candidate_shortlist()`: a position-fair round-robin over the same already-ranked, already-legality-filtered rows — computes no new score, never displaces the single best-ranked candidate, never excludes a position that is legitimately deepest (a real run on one position can still fill most of the slate once every other position's pool is exhausted). Reuses Team Score/Raw Action Value/Cost of Waiting/roster state/position caps exactly as before; this is a selection change only. 6 new unit/integration tests (position-fair coverage on a QB-heavy pool, best-candidate preservation, real-depth preservation, no rows invented/dropped, empty input, and an early/middle/late 1QB draft-state test built from real snake-order picks). **Verified live**: a fresh early-draft pick now shows a 2 QB / 2 RB / 2 WR / 2 TE mix instead of a QB-dominated slate.

**4. Opponent position demand in shortlist context.** Confirmed unchanged and not double-counted: `diversify_candidate_shortlist` only decides which already-legal rows get evaluated; it never touches `_roster_need_adjustment`/Make-It-Back's own demand-aware Monte Carlo, and the compact position-demand strip added in the first addendum continues to read from that same, single source.

**5-7. Player drawer redesign, uncertainty relabeling, compact news copy.** Redesigned to the requested compact structure: a primary grid (Pick Score headline; Team Score current→after→delta; Championship Equity after/delta; Make-It-Back; Cost of Waiting; Player Score; Market ADP; Action badge) with **Why / News / Details** as native, collapsed-by-default `<details>` sections. Research-development wording (EXPERIMENTAL/RESEARCH/SIMULATED RESEARCH, Raw Decision Utility component math) moved into Details/tooltips, never deleted from the system. The raw SE-derived uncertainty tier (e.g. "LOW_MODEL_UNCERTAINTY (SE=0.0000)") is no longer shown as an authoritative claim — Decision Confidence is a standing NOT_VALIDATED program verdict, so the drawer now reads **"Uncertainty: NOT VALIDATED"**, with the raw diagnostic string preserved only as a hover tooltip. News is now a compact "News — STALE (92h)" summary that expands for the full explanation; a quiet alert on a stale snapshot explicitly states this is not confirmed real-world clearance. **Verified live** (Ja'Marr Chase, a real player with a real current alert): drawer showed Pick Score 15.7, Team Score "0.0 → 5.0 (+5.0)", Championship Equity "1.7% (+1.67 pp)", Make-It-Back 0%, Cost of Waiting 0.0, Player Score 155.1, Market ADP —, Action "GOOD VALUE" — all in the compact primary grid with zero research wording visible; expanding News showed the real alert text plus the compact stale-data notice; expanding Details showed "Uncertainty: NOT VALIDATED" and the EXPERIMENTAL/RESEARCH disclosure text.

**8. Real GUI re-test.** All of the above (including a Draft Board re-check and a global-sidebar-collapse re-check) were rendered and interacted with live against the real isolated environment at the correct `#/draft-room-v2` HashRouter route, with before/during/after screenshots, not claimed from code inspection.

## Regression (this addendum)

- `tests/test_decision_bundle_live_service.py` + `_v2.py` + `test_decision_bundle_service.py` + `test_raw_action_value_live_service.py`: 25 passed (11 new/updated).
- `tests/test_desktop_application_api.py` + `test_desktop_application_api_decision_bundle_v2.py`: same 4 pre-existing baseline failures, zero new ones — including the previously-broken "V1 and V2 select the same candidates" test, now passing again because both use the identical diversified shortlist.
- Full desktop `vitest`: 104/104 pass. `npm run typecheck`: clean.
- Committed locally at `2ade72b9` (backend) and `f39b599b` (frontend), both on `work/nwr-draft-upgrade-hq-v1-20260903`. No push, no merge, no deploy.

## Final verdict (supersedes both verdicts above)

**`GREEN_OWNER_TEST_ISSUES_FIXED`**

All eight items in the owner's two finishing-pass reports were addressed and rendered-verified against the real, live, isolated GUI environment: horizontal primary tabs, no permanent second vertical column, a genuinely new (previously absent) global-sidebar collapse, an aggressively compacted context strip that lets Suggestions start near the top of the workspace, clean drafting-chrome table headers with evidence status moved to tooltips, a real Team Score Delta audit (confirmed correct math, fixed the presentation), restored continuous Pick Score/Team Score resolution at the live FAST preset (verified with real differentiated scores, not flat buckets), a real fix for QB-dominated Suggestions (verified balanced position mix), and a fully redesigned, compact, collapsible player drawer with the invalid uncertainty claim removed and compact expandable news. One real, disclosed, out-of-scope-for-this-pass limitation remains (a specific late-draft state where every candidate's simulated completion ties near the bottom of the reference population — a genuine tie, not hidden, not patched by inventing separation) and is named above for a dedicated follow-up rather than silently left undocumented.

No model tuning. No new Draft Room. No push/merge/deploy.

---

**UPDATE (same day, 20260906): a fourth owner-test pass (P0 workflow rescue / stabilization-reconstruction) found the consolidated room still required Legacy for slot selection and start/restart, had no real left utility pane, and had a large on-clock hero.** All fixed and rendered-verified; full write-up, golden-workflow evidence, and final verdict `GREEN_DRAFT_ROOM_GOLDEN_WORKFLOW_PASSES` are in the separate `NWR_DRAFT_ROOM_STABILIZATION_RECONSTRUCTION_V2_REPORT_20260906.md` (commit `5fb6a8c6`), per that pass's own explicit request for a distinctly-named report. This is the CURRENT, most up-to-date verdict for the consolidated Draft Room, superseding `GREEN_OWNER_TEST_ISSUES_FIXED` above.
