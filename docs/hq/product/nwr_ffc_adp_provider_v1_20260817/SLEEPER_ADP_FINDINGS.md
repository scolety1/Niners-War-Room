# Sleeper ADP Findings

`SUPPORTED SLEEPER ADP SOURCE: NONE FOUND`

The repository contains a display-context candidate at `C:\NWR_SHARED_DATA\lane_exchange\market_behavior\sleeper_adp_display_context\latest_candidate.json`, but no admitted `latest_approved.json`. The candidate is classified `YELLOW_UNDOCUMENTED_ENDPOINT`, display-only, and blocked from recommendations, simulations, and deployment.

This implementation does not call a Sleeper ADP API, does not use an undocumented private endpoint, does not browser-scrape Sleeper, and does not label FFC data as Sleeper data.
