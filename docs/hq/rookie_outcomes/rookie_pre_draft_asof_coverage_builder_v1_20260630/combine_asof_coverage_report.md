# Combine As-Of Coverage Report

Tracked NFLVerse player-context build report lists combine receipt rows: `650`.

Current player-context combine clean joins: `294` out of `294` current rows.

This does not prove historical drafted-only player-level combine coverage, event-date availability, extraction timestamp, or replay safety for the `1999` drafted audit rows.

Result: combine is a candidate only. `experiment_ready_now=false`, `model_use_allowed=false`, `training_allowed=false`, and `source_truth_allowed=false`.
