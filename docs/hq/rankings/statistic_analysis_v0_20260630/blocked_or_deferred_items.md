# Blocked Or Deferred Items

## Exact Contribution Percentages

Status: DEFER.

The current approved rankings artifact does not expose component weights, component scores, weighted contribution rows, or a receipt that reconciles component totals to `nwr_dynasty_score`.

This lane therefore shows `Contribution %` as `Not enough information`.

## Missing Component Count

Status: DEFER.

No approved component receipt artifact exists that can distinguish missing components from non-applicable, capped, blocked, or unavailable evidence. The main-table field displays `Not enough information`.

## Capped Component Count

Status: DEFER.

`confidence_cap` exists, but that is not an approved capped-component count. The main-table field displays `Not enough information`.

## Future Requirement

A future model/data lane can enable true decomposition only by producing an approved score receipt artifact with component names, weights, raw values, normalized values, caps, weighted contributions, missing semantics, source gates, and reconciliation to the displayed score.
