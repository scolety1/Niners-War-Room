# Preview table

Preview Parse shows Row, position rank, player, all four platform values, match status, matched NWR player, and reason/action. It defaults to 25 rows and provides All, Matched, Unmatched, Ambiguous, and Missing active platform filters.

The raw textarea remains monospace, whitespace-preserving, and secondary to the normalized evidence.
