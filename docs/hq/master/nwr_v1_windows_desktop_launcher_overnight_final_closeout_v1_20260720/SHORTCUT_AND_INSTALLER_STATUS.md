# Shortcut and installer status

The one-click installer is committed, independently approved, and disposable-root tested. It creates one Desktop launcher and eight Start Menu entries only from exact clean stable HQ after real-user identity, Known Folder, collision, and explicit confirmation gates. The real shortcut is not installed from the Codex sandbox. Status: `READY_FOR_ONE_CLICK_SHORTCUT_INSTALL_FROM_REAL_USER_ACCOUNT`.
