# Model evaluation tools

Prefer `scikit-learn` metrics/calibration, `scipy.stats` rank correlation, and `statsmodels` for transparent validation. Use holdout-by-week, Brier/log loss where probabilistic, pairwise/rank correlation, calibration curves and drift receipts. MLflow/Evidently are DEEP_INSPECT_LATER, not required infrastructure.
