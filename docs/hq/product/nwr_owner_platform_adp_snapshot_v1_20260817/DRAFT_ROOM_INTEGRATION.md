# Draft Room integration

Draft Room continues to load its existing ADP snapshot boundary. The global loader now resolves that boundary per active league, so recommendations, expected-pick context, and the compact source chip reflect the selected owner column.

NWR rank and projections remain separate and authoritative.
