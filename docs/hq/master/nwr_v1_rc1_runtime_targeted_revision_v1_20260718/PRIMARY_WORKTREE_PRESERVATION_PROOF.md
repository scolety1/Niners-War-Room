# Primary worktree preservation proof

`PRIMARY_USER_WORK_PRESERVED_AGAINST_RC2_LANE_START_BASELINE`

The user explicitly authorized the five current primary-worktree CSV byte states as this lane's preservation baseline. Only path, SHA-256, byte size, and filesystem timestamps were read; contents were never inspected.

Checks after control-state setup, route correction, full 180-case browser matrix, cycle 1, cycle 2, cycle 3, and focused gates found zero hash mismatch. Primary status remained exactly the same five modified paths and no additional primary-worktree change.

This proof preserves bytes. It does not canonicalize, validate, compare, interpret, or adopt their contents.
