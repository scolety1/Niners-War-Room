# Player identity matching tools

`rapidfuzz` is the preferred candidate-generator package: deterministic scores, no auto-join. `recordlinkage`, Splink and dedupe are DEEP_INSPECT_LATER; too heavy for initial local review queues. Every join retains a receipt and ambiguous cases require owner review.
