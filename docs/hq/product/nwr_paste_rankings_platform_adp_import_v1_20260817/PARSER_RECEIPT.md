# Parser receipt

Each parsed row preserves source row index, position/rank, player name, every supplied platform ADP, selected ADP/source, import metadata, and a SHA-256 raw-row hash. The original pasted text is preserved locally beside its snapshot.
