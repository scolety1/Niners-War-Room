# User Decision Options

## Options

1. Stop model work and inspect all findings manually.
2. Prepare docs-only push/merge-readiness for the locally canonicalized review packets.
3. Authorize a narrow second veteran-decline refinement despite mixed results.
4. Authorize a review-only overlay simulation readiness gate later, only after user review.
5. Pivot to non-model NWR work.
6. Reopen data acquisition only for a specific source, such as historical/as-of market, with explicit user approval.

## Short Recommendation

Stop active model work for now, manually inspect sparse-history and veteran-decline findings, then decide whether to do docs-only push/merge-readiness.

Do not authorize ranking simulation, production/model-use, rankings integration, app/runtime changes, source promotion, push/merge, canonical `local_exports` mutation, or hidden sort/recommendation logic from this packet alone.
