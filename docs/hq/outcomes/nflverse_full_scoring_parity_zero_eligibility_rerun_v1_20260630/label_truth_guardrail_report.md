# Label Truth Guardrail Report

This packet does not promote label truth, model use, training, source truth, or experiments.

Every generated CSV row keeps:

- `label_truth_allowed=false`
- `model_use_allowed=false`
- `training_allowed=false`
- `source_truth_allowed=false`
- `experiment_allowed=false`
- `review_only=true`

Existing labels remain evaluation targets. The sidecar and zero-eligibility rows remain review-only comparison evidence.
