# Launcher canonical status

Repository candidate is accepted V1 HQ plus the exact launcher chain and correction `e5df0f29ef7530077cd941702e37b7977c286c8a`. Final push is authorized only after a last fetch/readback and normal fast-forward feasibility check. No force push, tag, or Git automation is permitted.

Canonical runtime contract is `127.0.0.1:8520`, `pythonw.exe` start, verified ownership, health-based reuse, bounded owned stop, and user state outside Git.
