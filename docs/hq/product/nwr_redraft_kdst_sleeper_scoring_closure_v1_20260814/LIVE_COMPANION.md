# Live Sleeper companion

The prior lane's read-only `draft/{draft_id}/picks` reader is preserved. It may safely inform local depletion only after the complete board gate clears. It does not make real picks, transactions, roster changes, or automated Sleeper interactions.
