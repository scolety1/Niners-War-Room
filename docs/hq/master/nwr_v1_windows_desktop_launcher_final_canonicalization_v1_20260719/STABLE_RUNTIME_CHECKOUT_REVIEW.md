# Stable runtime checkout review

Required path: `C:\NWR\Niners-War-Room-V1`. It must be a standalone clone with a `.git` directory, exact final pushed `origin/work/hq-parallel-control`, and a clean tree. LocalData and synthetic state must not be copied. User data stays under per-user/shared roots outside the checkout.

At documentation-commit time this is a gated post-push action and is not yet claimed complete. Final task handoff must report its actual commit/tree/readback or the exact conflict verdict.
