# Rollback plan

The successor is one local commit directly above RC `c380956dcab863dd7f921ec237506c08f46990b8`. It is not pushed or tagged.

Before adoption, rollback is simply refusal to adopt the local successor; canonical HQ and the RC remain unchanged. After ordinary adoption, use the repository's normal non-history-rewriting revert procedure to revert the single successor commit. Do not force push, amend, reset protected worktrees, or restore over evidence.

The rollback unit includes the preserved route/runtime candidate, the narrow Data Health classifier correction, directly related tests, the preserved prior RED packet, and this successor evidence packet. Reverting only the classifier would recreate the deterministic documentation-manifest failure and is not an accepted partial rollback.

The five RC2 primary CSVs are outside the commit and must continue to match their preservation hashes before and after any adoption or rollback action.
