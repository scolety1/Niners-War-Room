# Protected and Frozen Path Proof

The candidate intentionally changes only Codex security automation, repository verification scripts, tests/fictional fixtures, `.gitattributes` scoped to the fixture contract, and this packet.

No path under `app/`, `src/`, `data_packs/`, production export packs, ranking/formula configuration, source-admission policy, freshness policy, or `docs/codex/PROFILE.json` changed. No dependency manifest changed. The one existing UI test change corrects a false positive and changes no implementation behavior.

The primary worktree's five unrelated DynastyProcess CSV modifications remained untouched throughout.
