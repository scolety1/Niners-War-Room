{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dynasty-main-runtime",
  "description": "Allows the bundled Dynasty main window to request only its local API session descriptor.",
  "local": true,
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "allow-desktop-runtime",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
