# Matching gap diagnostics

Unmatched preview rows retain their parsed ADP values and a safe reason. The matcher now distinguishes position mismatch and ambiguous name cases from no safe identity match where evidence permits. No automatic identity joins were added.
