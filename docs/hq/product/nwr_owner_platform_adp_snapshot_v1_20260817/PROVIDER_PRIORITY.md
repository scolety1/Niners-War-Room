# Provider priority

For an enabled owner platform snapshot, each player uses the selected league column, then Consensus, then the local FFC cache. If neither is available, ADP is unavailable. Owner CSV remains below FFC in the pre-existing provider order.

Disabled/use FFC bypasses the global snapshot for that league. No pasted column is represented as an API result.
