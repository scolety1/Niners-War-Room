# Privacy, Rights, and No-Network Proof

Tracked fixtures contain eleven tiny rows, all labeled `FICTIONAL_TEST_FIXTURE_NOT_REAL`, with only `fictional_*` identifiers and arithmetic behavior values. They do not claim historical truth, provider content, player identity, rankings, league state, production evidence, or completeness.

The bootstrap contains no HTTP URL or network invocation primitive and sets `networkRequired` false. Repository verification invokes dependency tooling with both `--offline` and `--no-project`. No credentials, private data, licensed rows, provider exports, real histories, plugin outputs, or arbitrary local paths were consumed.

The names of F06-F08 document the excluded rights boundary; their payload rows are fictional schema behavior only and are not reconstructions of excluded data.
